services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-bot
    ports:
      - "3999:3000"
    env_file:
      - .env
    volumes:
      - .wwebjs_auth:/home/node/.wwebjs_auth  # Para que la sesión de WhatsApp se mantenga
    restart: unless-stopped
